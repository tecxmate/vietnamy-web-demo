--- src/pages/Practice/ToneMarks.css
+++ src/pages/Practice/ToneMarks.css
@@ -1,81 +1,3 @@
 /* src/pages/ToneMarks.css */
 
-.tonemarks-container {
-    padding: var(--spacing-xl) 0;
-    max-width: 800px;
-    margin: 0 auto;
-    width: 92%;
-    min-height: 80vh;
-    display: flex;
-    flex-direction: column;
-}
-
-/* Header */
-.tonemarks-header {
-    display: flex;
-    justify-content: space-between;
-    align-items: center;
-    margin-bottom: var(--spacing-xl);
-}
-
-.tonemarks-header h1 {
-    display: flex;
-    align-items: center;
-    gap: 12px;
-    margin: 0;
-    color: var(--color-primary);
-    font-size: 1.8rem;
-}
-
-.tonemarks-stats {
-    display: flex;
-    gap: var(--spacing-md);
-}
-
-.tonemarks-stat-pill {
-    display: flex;
-    align-items: center;
-    gap: 6px;
-    padding: 6px 12px;
-    border-radius: 20px;
-    font-weight: 600;
-    font-size: 0.9rem;
-}
-
-.tonemarks-stat-pill.score {
-    background: var(--color-gold-light, #fff8e1);
-    color: var(--color-gold-dark, #b7950b);
-}
-
-.tonemarks-stat-pill.streak {
-    background: var(--color-fire-light, #ffebee);
-    color: var(--color-fire-dark, #c62828);
-}
-
-/* Stage tabs */
-.tm-stage-tabs {
-    display: flex;
-    gap: var(--spacing-sm);
-    margin-bottom: var(--spacing-lg);
-}
-
-.tm-stage-tab {
-    flex: 1;
-    padding: 10px 12px;
-    border: 2px solid var(--color-border);
-    border-radius: var(--radius-lg);
-    background: var(--color-surface);
-    font-weight: 600;
-    font-size: 0.85rem;
-    cursor: pointer;
-    transition: all 0.2s;
-    text-align: center;
-    color: var(--color-text-muted);
-}
-
-.tm-stage-tab:hover {
-    border-color: var(--color-primary);
-    color: var(--color-text);
-}
-
-.tm-stage-tab.active {
-    background: var(--color-primary);
-    color: white;
-    border-color: var(--color-primary);
-}
-
 /* ─── Stage 1: Periodic Table ─── */
@@ -87,4 +9,4 @@
 .periodic-table {
     overflow-x: auto;
-    -webkit-overflow-scrolling: touch;
     margin-bottom: var(--spacing-lg);
 }
@@ -102,4 +102,5 @@
     padding: 8px 4px;
     font-weight: 700;
     font-size: 0.8rem;
     text-align: center;
@@ -139,5 +139,5 @@
     cursor: pointer;
     border-radius: 6px;
-    background: var(--color-surface);
     border: 2px solid transparent;
     transition: all 0.15s;
     color: var(--color-text);
@@ -148,4 +148,5 @@
     border-color: var(--color-primary);
     transform: scale(1.08);
+    background: var(--surface-color);
     box-shadow: var(--shadow-md);
     z-index: 1;
@@ -160,5 +161,5 @@
 .periodic-cell.playing {
     border-color: var(--color-primary);
-    background: rgba(var(--color-primary-rgb), 0.1);
+    background: rgba(255, 209, 102, 0.15); /* var(--primary-color) with opacity */
     animation: cellPulse 0.4s ease;
 }
@@ -172,5 +173,5 @@
 .periodic-cell.no-audio {
     opacity: 0.45;
-    cursor: default;
+    cursor: pointer; /* Change to pointer so it behaves like disabled */
     color: var(--color-text-muted);
 }
@@ -220,44 +221,4 @@
 }
 
-/* ─── Stage 2 & 3: Quiz Card ─── */
-.tm-quiz-card {
-    background: var(--color-surface);
-    border: 1px solid var(--color-border);
-    border-radius: var(--radius-xl);
-    padding: var(--spacing-xl);
-    box-shadow: var(--shadow-md);
-    text-align: center;
-    min-height: 420px;
-    display: flex;
-    flex-direction: column;
-    position: relative;
-}
-
-.tm-progress {
-    height: 8px;
-    background: var(--color-border);
-    border-radius: 4px;
-    overflow: hidden;
-    margin-bottom: var(--spacing-xl);
-}
-
-.tm-progress-fill {
-    height: 100%;
-    background: linear-gradient(90deg, var(--color-primary), #58cc02);
-    transition: width 0.4s ease;
-}
-
-.tm-quiz-content {
-    flex: 1;
-    display: flex;
-    flex-direction: column;
-    justify-content: center;
-    align-items: center;
-    gap: var(--spacing-lg);
-}
-
 /* Combine prompt */
 .tm-equation {
@@ -271,7 +232,7 @@
 
 .tm-base-vowel {
-    color: var(--color-primary);
-    background: rgba(var(--color-primary-rgb), 0.08);
+    color: var(--primary-color);
+    background: rgba(255, 209, 102, 0.08);
     padding: 8px 20px;
     border-radius: var(--radius-lg);
-    border: 2px solid rgba(var(--color-primary-rgb), 0.2);
+    border: 2px solid rgba(255, 209, 102, 0.2);
 }
@@ -321,5 +282,5 @@
 .tm-meaning {
     font-size: 0.95rem;
-    color: var(--color-text-secondary, #666);
-    background: rgba(var(--color-primary-rgb), 0.06);
+    color: var(--text-muted);
+    background: rgba(255, 209, 102, 0.06);
     padding: 6px 16px;
     border-radius: 20px;
@@ -335,5 +296,5 @@
 
 .tm-option {
-    padding: 16px 8px;
-    background: var(--color-background);
+    padding: 24px 8px; /* Increased padding */
+    background: transparent;
     border: 2px solid var(--color-border);
     border-radius: var(--radius-lg);
@@ -342,5 +303,5 @@
     font-size: 1.5rem;
     font-weight: 600;
-    color: var(--color-text);
+    color: var(--text-main);
 }
@@ -351,11 +312,12 @@
 .tm-option:hover {
-    border-color: var(--color-primary);
+    border-color: var(--primary-color);
     transform: translateY(-2px);
+    background: var(--surface-color);
 }
 
 .tm-option.selected {
-    border-color: var(--color-primary);
-    background: rgba(var(--color-primary-rgb), 0.08);
-    box-shadow: 0 0 0 3px rgba(var(--color-primary-rgb), 0.12);
+    border-color: var(--primary-color);
+    background: rgba(255, 209, 102, 0.08);
+    box-shadow: 0 0 0 3px rgba(255, 209, 102, 0.15);
 }
@@ -370,127 +332,5 @@
     border-color: #ff4b4b;
-    background: rgba(255, 75, 75, 0.1);
+    background: rgba(239, 71, 111, 0.1);
     color: #ea2b2b;
 }
 
 .tm-option.disabled {
     pointer-events: none;
     opacity: 0.5;
-}
-
-/* Footer */
-.tm-footer {
-    padding: var(--spacing-lg) 0 0;
-    display: flex;
-    justify-content: space-between;
-    align-items: center;
-    gap: var(--spacing-md);
-    margin-top: auto;
-}
-
-.tm-feedback-msg {
-    font-weight: 600;
-    font-size: 1.05rem;
-    display: flex;
-    align-items: center;
-    gap: 10px;
-}
-
-.tm-btn-check,
-.tm-btn-continue {
-    padding: 12px 30px;
-    border-radius: 12px;
-    font-weight: 600;
-    font-size: 1rem;
-    cursor: pointer;
-    border: none;
-    text-transform: uppercase;
-    letter-spacing: 0.5px;
-    margin-left: auto;
-}
-
-.tm-btn-check {
-    background-color: #58cc02;
-    color: white;
-    box-shadow: 0 4px 0 #58a700;
-}
-
-.tm-btn-check:active {
-    box-shadow: none;
-    transform: translateY(4px);
-}
-
-.tm-btn-check:disabled {
-    background-color: var(--color-border);
-    color: var(--color-text-muted);
-    box-shadow: none;
-    cursor: not-allowed;
-}
-
-.tm-btn-continue {
-    background-color: #58cc02;
-    color: white;
-    box-shadow: 0 4px 0 #58a700;
-}
-
-.tm-btn-continue.incorrect {
-    background-color: #ff4b4b;
-    box-shadow: 0 4px 0 #ea2b2b;
-}
-
-/* Summary */
-.tm-summary-card {
-    text-align: center;
-    background: var(--color-surface);
-    padding: var(--spacing-xxl);
-    border-radius: var(--radius-xl);
-    box-shadow: var(--shadow-lg);
-    animation: tmSlideUp 0.5s ease;
-    flex: 1;
-    display: flex;
-    flex-direction: column;
-    justify-content: center;
-    align-items: center;
-}
-
-.tm-summary-card .trophy-icon {
-    color: var(--color-gold-dark, #b7950b);
-    margin-bottom: var(--spacing-lg);
-}
-
-.tm-final-score {
-    font-size: 2.5rem;
-    font-weight: 800;
-    color: var(--color-primary);
-    margin: var(--spacing-md) 0;
-}
-
-.tm-summary-text {
-    font-size: 1.2rem;
-    color: var(--color-text-secondary);
-    margin-bottom: var(--spacing-xl);
-}
-
-.tm-action-buttons {
-    display: flex;
-    justify-content: center;
-    gap: var(--spacing-md);
-    flex-wrap: wrap;
-}
-
-.tm-action-buttons .btn-primary,
-.tm-action-buttons .btn-secondary {
-    display: flex;
-    align-items: center;
-    gap: 8px;
-    padding: 12px 24px;
-    border-radius: 30px;
-    font-weight: 600;
-    cursor: pointer;
-    text-decoration: none;
-    transition: transform 0.2s;
-    border: none;
-    font-size: 1rem;
-}
-
-.tm-action-buttons .btn-primary {
-    background: var(--color-primary);
-    color: white;
-}
-
-.tm-action-buttons .btn-secondary {
-    background: var(--color-surface-variant);
-    color: var(--color-text);
-}
-
-.tm-action-buttons .btn-primary:hover,
-.tm-action-buttons .btn-secondary:hover {
-    transform: translateY(-2px);
-}
-
-@keyframes tmSlideUp {
-    from {
-        transform: translateY(20px);
-        opacity: 0;
-    }
-
-    to {
-        transform: translateY(0);
-        opacity: 1;
-    }
-}
-
-@media (max-width: 560px) {
